# DLUT 校园网一键修复 · 使用说明

| 项 | 内容 |
|---|---|
| 配套文件 | `DLUT校园网一键修复_2026-09-15.bat` |
| 适用 | 大连理工大学校园网（凌水校区 `DLUT-LingShui`、盘锦校区 `DLUT-panjin`） |
| 版本 | v2026-09-15，基于 2026-09-14 一次真实故障排查复现 |
| 系统要求 | Windows 10 / 11 |

> **一句话**：连不上校园网、认证页显示「网络中断」时，先双击跑一次诊断；如果输出里指明是「无线配置损坏」或「hosts 被硬编码」，再跑修复。

---

## 1. 先判断：你的情况它管不管

| 你的现象 | 它能不能修 |
|---|---|
| 认证页提示「网络中断」、「不在 IP 地址」 | ✅ 大概率能修（这正是它的目标场景） |
| IP 变成 `169.254.x.x` | ✅ 大概率能修 |
| 昨天还能用，今天突然连不上，同一台电脑 | ✅ 值得一试 |
| 提示账号欠费 / 流量用尽 / 账号被锁 | ❌ 去 [tulip.dlut.edu.cn](http://tulip.dlut.edu.cn) 自查缴费 |
| 室友也连不上、整栋楼都连不上 | ❌ 是楼宇交换机或校园网故障，直接报修 |
| 要输入账号密码才能连的 Wi-Fi（802.1X 类） | ❌ 本脚本只处理开放式 + 网页认证的网络 |
| 无线网卡在设备管理器里是黄叹号 | ❌ 是驱动问题，需重装驱动 |

---

## 2. 怎么跑

### 方式一：双击（推荐）

双击 `DLUT校园网一键修复_2026-09-15.bat`，会弹出菜单：

```
  [1] 只诊断    不改任何配置，不需要管理员，约 10 秒出结果
  [2] 一键修复  重建无线配置 + 清理 hosts，需要管理员权限

  按 1 只诊断 / 按 2 一键修复（15 秒不选则按只诊断处理）:
```

- **按 `2`** 修复 → 会弹出 UAC 提权窗口，点「是」
- **按 `1`** 只诊断 → 不弹 UAC，10 秒左右出结果
- 按键即可，不用敲回车；**15 秒不操作会自动按「只诊断」走**，不会改动任何东西
- 建议顺序：**先按 1 看诊断结果，再按 2 修复**

### 方式二：命令行（可选，参数更可控）

```bat
DLUT校园网一键修复_2026-09-15.bat -diag        :: 只诊断，免管理员
DLUT校园网一键修复_2026-09-15.bat -fix         :: 直接修复，自动提权
DLUT校园网一键修复_2026-09-15.bat -fix -y      :: 修复且不再问「是否切回原网络」
```

| 参数 | 作用 |
|---|---|
| `-diag` | 只诊断，不修改任何配置，不需要管理员 |
| `-fix` | 直接进入修复，不弹菜单 |
| `-y` | 跳过交互确认（包含结束时「是否切回原网络」） |
| `-panjin` / `-lingshui` | 手动指定校区常量（默认会自动扫描判断） |
| `-nopause` | 结束后不停留，直接退出 |

---

## 3. 它会动你什么（重点看这里）

修复模式一共 8 步，**所有改动都会先备份**：

| 步骤 | 做什么 | 是否会改你的东西 |
|---|---|---|
| 1 | 体检网卡、当前所连 SSID、IP | 只读 |
| 2 | 备份 hosts 和当前无线配置 | 只新增备份文件 |
| 3 | 检查 WLAN 服务、读取配置状态 | 只读 |
| 4 | 检查 hosts 里有没有被写死的认证域名 IP | **会**：把失效记录注释掉（不删除） |
| 5 | 重建 `DLUT-LingShui` 的无线配置 | **会**：删除后重建这一个配置 |
| 6 | 连接校园网，检查是否拿到 IP | **会**：可能断开你当前连的 Wi-Fi |
| 7 | 检查网关、认证服务器、域名解析、认证页 | 只读 |
| 8 | 打开认证页，打印备份与回滚命令 | 只打开浏览器 |

几个你可能会担心的点：

- **备份在哪里**：脚本同目录的 `backup\` 文件夹（`hosts.bak`、`WLAN-DLUT-LingShui.xml`）。若该目录不可写，会退到 `%TEMP%\dlut-net-backup`。
- **hosts 只是注释，不是删除**：被处理的那行会变成
  `# disabled 2026-09-15 stale hardcoded portal IP: 202.118.66.117 auth.dlut.edu.cn`，随时可还原。
- **只动认证域名**：正则要求行首是 IP 且 `auth.` 前面有空格，所以 `oauth.xxx` 这类无关记录不会被误伤；其它 `*.dlut.edu.cn` 记录只报告、不修改。
- **不会影响别的网卡**：第 6 步的释放/续租**只作用于无线网卡**（实测确认不会碰有线、VPN、热点这些网卡的租约）。
- **只删一个无线配置**：只处理 `DLUT-LingShui`（或盘锦的 `DLUT-panjin`），你家的 Wi-Fi、手机热点配置都不会被动。
- **会断开当前 Wi-Fi**：如果当时挂在手机热点上，脚本会先问你确认（加 `-y` 则不问）。

---

## 4. 输出怎么读

| 标记 | 含义 |
|---|---|
| `[OK]` | 这一项正常 |
| `[INFO]` | 说明性信息，通常不用管 |
| `[WARN]` | 有异常但不致命，脚本可能已跳过某项 |
| `[FAIL]` | 这一项失败，需要按提示处理 |

几个关键判读：

- `WLAN IPv4` 是 `10.x` 或 `172.x` → 拿到校园网地址了，正常
- `WLAN IPv4` 是 `169.254.x.x` → **没拿到 IP**，这正是「网络中断」的直接原因
- `认证页 HTTP 302` → 认证页劫持生效，链路已通，去浏览器登录即可
- `认证页无响应` → 已登录认证的状态下不响应属正常，不用紧张

---

## 5. 常见情形对照

| 诊断输出里看到 | 结论 | 怎么办 |
|---|---|---|
| `没有找到 DLUT-LingShui 的无线配置` | 无线配置丢了 | 跑修复重建 |
| `[异常] ... auth.dlut.edu.cn` + `当前不是管理员，跳过` | 发现 hosts 硬编码，但没提权 | 以管理员身份重跑，或用 `-fix` |
| `没拿到有效地址` → `续租成功后拿到 10.x` | DHCP 一度异常，续租已恢复 | 正常登录即可 |
| `没拿到有效地址`，续租后仍是 `169.254.x.x` | 账号侧或交换机侧问题 | 见下一节第 1、4 条 |
| `当前所连SSID : <你的手机名>` | 排查时你根本没在校园网上 | 属正常提示，不影响修复 |
| `范围内没扫到校内 SSID` | 你不在校区（宿舍用热点/在家） | 属正常，修复动作照样可用于"提前修好" |

---

## 6. 修完还要做两件事

1. **手动连一次校园网**：重建后的配置是**手动连接**模式（避免校园网抽风时把电脑从热点上拽走），所以要点任务栏 Wi-Fi 图标 → 选 `DLUT-LingShui` → 连接。
2. **浏览器登录**：脚本会自动打开 `http://auth.dlut.edu.cn`，没有的话手动开任意 HTTP 网页也会跳转；输入学号 + 密码，选线路登录。

想改回**自动连接**：设置 → 网络和 Internet → WLAN → 管理已知网络 → `DLUT-LingShui` → 勾选「自动连接」。

---

## 7. 想反悔？回滚很简单

脚本每次跑完都会在屏幕下方打印回滚命令，也可以直接照抄下面两条：

```bat
:: 还原 hosts（需管理员）
copy /y "backup\hosts.bak" "C:\Windows\System32\drivers\etc\hosts"

:: 还原原来的无线配置
netsh wlan add profile filename="backup\WLAN-DLUT-LingShui.xml" user=all
```

（把 `backup\` 换成脚本所在目录的实际路径）

---

## 8. 还是不通？按顺序来

1. **账号侧清绑定**：登录 [tulip.dlut.edu.cn](http://tulip.dlut.edu.cn) → 自助服务 → 认证上网服务 → 在线信息 → **把全部设备强制下线**，再重新认证。这是学校官方推荐做法，能清掉账号侧的旧绑定。
2. **确认套餐流量是否用完、账号是否欠费。**
3. **关掉下载器 / BT / 多开浏览器**（本机出现过「全局短端口耗尽」，会拖慢认证页）。
4. **报修**：网信中心服务热线 **84707007**，或 i大工 App → 网络自助 提报工单。报修时把脚本 `-diag` 的输出一起给对方，能省很多沟通。

---

## 9. 常见问题

**Q：会不会丢我的 Wi-Fi 密码？**
不会。`DLUT-LingShui` 是**开放式网络，本来就没有密码**；这个配置重建前后都不涉及密码。你其它 Wi-Fi 的密码也完全不受影响。

**Q：会不会影响我家的 Wi-Fi / 手机热点？**
不会。脚本只删并重建 `DLUT-LingShui` 这一个配置，也不会去动其它网卡的 IP。唯一影响是：修复过程中它会连到校园网，可能把你从当前 Wi-Fi 上切走（脚本会先问你，结束时还会问要不要切回去）。

**Q：我没有管理员权限（比如公司/借来的电脑）怎么办？**
跑 `-diag` 诊断没问题，但修复会因缺权限而跳过（脚本会明确提示，且**不会**在不提权的情况下先删配置，所以不会把配置删坏）。没有管理员权限时请按第 10 节手动处理。

**Q：Mac / 手机 / Linux 能用吗？**
不能，这是 Windows 专用脚本。Mac 的等价操作见第 10 节末尾。

**Q：每次上网都要跑一遍吗？**
不用。它是**修一次就好**的工具，只在出问题时跑。日常上网就是点 Wi-Fi + 浏览器登录。

**Q：它会不会误改我 hosts 里别的东西？**
不会。它只匹配「行首是 IP 地址、后面跟着 `auth.` 域名」的有效行，而且是**注释**而非删除，原文件也已备份。

**Q：这东西可靠吗？改的内容我能核对吗？**
脚本的全部动作都可在文本编辑器里逐行读（纯 bat + 内联 PowerShell，没有加密和联网下载）。它复现自 2026-09-14 一次真实故障排查，完整取证记录见配套的《DLUT校园网认证故障排查记录》，里面所有 IP、时间戳、事件 ID 都是实测原始值。

---

## 10. 手动版（不想跑脚本时）

以管理员身份做这三件事即可，效果等价：

```bat
:: 1) 忘记并重连校园网
::    设置 → 网络和 Internet → WLAN → 管理已知网络 → DLUT-LingShui → 忘记
::    然后重新点击 DLUT-LingShui 连接

:: 2) 清掉 hosts 里写死的认证域名（用记事本以管理员打开 C:\Windows\System32\drivers\etc\hosts）
::    把形如  202.118.66.117 auth.dlut.edu.cn  的行前面加 # 注释掉
::    （注意：真正的认证地址是 202.118.66.137，不是 .117）

:: 3) 刷新租约
ipconfig /release "WLAN"
ipconfig /renew "WLAN"
ipconfig /flushdns
```

**Mac**：用 `sudo nano /etc/hosts` 注释掉那条 `auth.*` 记录 → 在「系统设置 → 网络 → 详细信息 → 忘记此网络」后重新连接。

---

## 11. 附：脚本用的常量（可核查）

| 项 | 值 |
|---|---|
| 校园网 SSID | `DLUT-LingShui`（凌水）／`DLUT-panjin`（盘锦） |
| 认证页 | `auth.dlut.edu.cn` |
| 认证服务器 | `202.118.66.137` ← **注意不是 `.117`** |
| 校内网段 / 网关 | `10.5.0.0/16`，网关 `10.5.0.1` |
| 自助服务 | `tulip.dlut.edu.cn` |
| 网信中心热线 | 84707007（盘锦 0427-2631978） |

> 盘锦校区的常量来自公开信息，**未经实测**；脚本运行时会打印提示。若与实际不符，改脚本头部的常量即可。

---

## 12. 转发给同学时，可以只发这一段

> 校园网连不上、认证页显示「网络中断」的，可以试试这个一键修复（Windows）：双击 `DLUT校园网一键修复_2026-09-15.bat` → 按 `1` 先诊断，10 秒出结果。如果提示「无线配置损坏」或「hosts 被硬编码」就按 `2` 修复（会弹 UAC 点「是」，所有改动都有备份，随时可回滚）。账号欠费、整栋楼都连不上这类问题它修不了，直接打 84707007。
